# AI-SQL-Agent-
Converts plain English questions into SQL queries, runs them on a real SQLite database, and explains results in plain English. Write operations require human approval before execution.

## Tech: LangGraph · LangChain · Groq · SQLite · Streamlit · Python

Run:

``pip install langgraph langchain-groq python-dotenv streamlit``

``streamlit run app.py``
